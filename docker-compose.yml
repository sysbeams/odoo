version: '3.1'
services:
  web:
    image: odoo
    build: .
    depends_on:
      - db
    ports:
      - "8080:80"
  db:
    image: postgres:13
    ports:
      - "5432:5432"
    env_file:
      - ./.env
